node_modules
dist
dist-ssr
.output
.nitro
.tanstack
.vite
.wrangler
*.local
*.log
.env
.env.*
# The template is meant to be committed; the filled-in copies are not.
!.env.example
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json

# Scratch written by the Supabase CLI. Not ours, not useful in the repo.
supabase/.temp/
